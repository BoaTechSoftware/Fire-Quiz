let mode='login';const $=s=>document.querySelector(s);function message(text=''){const el=$('#formError');el.textContent=text;el.hidden=!text;}document.querySelectorAll('.portal-tab').forEach(button=>button.onclick=()=>{mode=button.dataset.mode;document.querySelectorAll('.portal-tab').forEach(x=>x.classList.toggle('active',x===button));$('#authTitle').textContent=mode==='login'?'Content de te revoir.':'Crée ton espace.';$('#authLead').textContent=mode==='login'?'Connecte-toi pour retrouver tes révisions.':'Commence gratuitement, sans profil public.';$('#submitButton').textContent=mode==='login'?'Se connecter':'Créer mon compte';$('#password').autocomplete=mode==='login'?'current-password':'new-password';message();});$('#authForm').onsubmit=async event=>{event.preventDefault();message();const button=$('#submitButton');button.disabled=true;button.textContent='Patiente…';try{const response=await fetch(`/api/auth/${mode}`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({email:$('#email').value,password:$('#password').value})});const data=await response.json();if(!response.ok)throw Error(data.error||'Une erreur est survenue.');window.location.href=data.role==='admin'?'/admin.html':'/';}catch(error){message(error.message);}finally{button.disabled=false;button.textContent=mode==='login'?'Se connecter':'Créer mon compte';}};
